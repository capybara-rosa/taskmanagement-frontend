apiVersion: v1
kind: Service
metadata:
  name: task-manager
  namespace: task-manager
  labels:
    app.kubernetes.io/name: task-manager
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: task-manager
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
